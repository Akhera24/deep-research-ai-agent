# ============================================================================
# Deep Research AI Agent - Dependencies
# ============================================================================
# Direct dependencies only (transitive deps resolved by pip).
# Load-bearing pins are exact working-venv floors (repinned 2026-07-07);
# legacy utility floors are loose-but-satisfiable.
#
# Install:
#   pip install -r requirements.txt
# ============================================================================

# ── Web framework (API serving; Phase 3 deploy) ──
fastapi>=0.129
uvicorn[standard]>=0.27
python-multipart>=0.0.6

# ── Agent orchestration ──
langgraph>=1.0
langgraph-checkpoint>=4.0   # optional SqliteSaver path in src/core/workflow.py
langchain-core>=1.2         # langgraph runtime dep, pinned deliberately

# ── AI model clients ──
anthropic>=0.83
openai>=2.21
google-genai>=1.64          # replaces google-generativeai (EOL 2025-11-30)
tiktoken>=0.12              # direct dep: unguarded import in claude_client.py / openai_client.py

# ── HTTP & async ──
aiohttp>=3.13               # search executor
httpx>=0.26                 # Phase 3: Turnstile siteverify
requests>=2.31

# ── Web scraping & parsing ──
beautifulsoup4>=4.12

# ── Database ──
sqlalchemy[asyncio]>=2.0.46  # [asyncio] pulls greenlet, required by the async engine
psycopg2-binary>=2.9        # legacy sync src/database/ module
asyncpg>=0.29               # async Postgres driver (API job table on Railway)
aiosqlite>=0.20             # async sqlite driver (API local dev)

# ── Data validation & settings ──
pydantic>=2.12
pydantic-settings>=2.13
python-dotenv>=1.0

# ── Retry logic ──
tenacity>=8.2

# ── Logging ──
structlog>=24.1

# ── Phase 3: rate limiting, sanitization, SSE, templating ──
slowapi>=0.1.9              # per-IP rate limiting
bleach>=6.1                 # HTML sanitization of scraped facts (stored-XSS guard)
sse-starlette>=2.1          # SSE progress streams
markupsafe>=2.1             # direct pin: load-bearing for HTML escaping in reports
jinja2>=3.1                 # server-rendered index page

# ── Utilities ──
python-dateutil>=2.8

# ── Testing ──
pytest>=7.4
pytest-asyncio>=0.23
pytest-cov>=4.1

# ── Code quality ──
mypy>=1.8
black>=24.1
ruff>=0.1.13

# ── Optional (install if needed) ──
# sentry-sdk>=1.40  # error tracking — enable when SENTRY_DSN is configured
