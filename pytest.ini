[pytest]
# Default run excludes the browser suite so the fast gate stays fast
# (PLAN.md Rev 2.1, Step A3). Run the browser suite with: pytest -m e2e
addopts = -m "not e2e"
markers =
    e2e: browser-driven end-to-end tests (pytest-playwright + chromium; dev-only, see requirements-dev.txt)
